<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.artera.movieexpertdagger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-sdk tools:overrideLibrary="androidx.security"/>

    <application
        android:name="com.artera.movieexpertdagger.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieExpert">
        <activity android:name="com.artera.movieexpertdagger.splash.SplashActivity"
            android:theme="@style/Theme.MovieExpert.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.artera.movieexpertdagger.detail.DetailActivity"
            android:theme="@style/Theme.MovieExpert.NoActionBar" />
        <activity
            android:name="com.artera.movieexpertdagger.MainActivity"
            android:theme="@style/Theme.MovieExpert.NoActionBar">

        </activity>
    </application>

</manifest>